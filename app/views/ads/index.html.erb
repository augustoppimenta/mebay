 <h1>Todos os anúncios</h1>
  <ul>
    <% for ad in @ads %>
      <li><a href="/ads/<%= ad.id %>"><%= ad.name %></a>
      [<a href="/ads/<%= ad.id %>/delete">Excluir</a>]</li>
    <% end %>
  </ul>
